---
import MenuPage from "../../../components/MenuPage.astro";
import { getMenuProfile, getMenuSections } from "../../../utils/menuContent";

const profile = await getMenuProfile("corpo");
const sections = await getMenuSections("corpo");
---

<MenuPage profile={profile} sections={sections} />
