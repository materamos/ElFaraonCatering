---
import MenuPage from "../../../components/MenuPage.astro";
import { getMenuProfile, getMenuSections } from "../../../utils/menuContent";

const menuId = "corpo";
const profile = await getMenuProfile(menuId);
const sections = await getMenuSections(menuId);
---

<MenuPage menuId={menuId} profile={profile} sections={sections} />
